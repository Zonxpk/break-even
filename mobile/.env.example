# Supabase Cloud — get URL and anon key from Dashboard → Settings → API
# Project ref example: sgxdoukyzaluwkbaxqvy
#
# Vercel (project: break-even): add these as Production env vars in the dashboard.
# They are required at build time for static web export (EXPO_PUBLIC_*).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
